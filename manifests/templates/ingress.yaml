---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 56                  # Ingress name
  labels:
    app: 56
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "56.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: 56               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
